db_config = {
    'host': "localhost",
    'user': "root",
    'password': "Global@12",
    'port': 3307
    #'database': "parliament_data" #we were facinga n error before because we wrote database: parliament_data which we were trying to create  and the code was having conflict due to which it couldnot run
    # ⚠️ Notice: no "database" here, because we want to create parliament_data if missing

}
